[project]
name = "weekly-investment-agent-local"
version = "0.1.0"
description = "Local, review-gated Taiwan stock research agent"
requires-python = ">=3.10"
dependencies = ["openai-agents>=0.6.0"]

[tool.uv]
package = false
